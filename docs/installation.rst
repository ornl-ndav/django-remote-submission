============
Installation
============

At the command line::

    $ easy_install django-remote-submission

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-remote-submission
    $ pip install django-remote-submission
